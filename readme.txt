1.cmdDemo是纯C++项目,内容包括：ShellExecute执行指令；枚举；字符数组的遍历。

2.CopyFile是纯C++项目,复制文件夹以及里面的文件。

3.mythreadPool创建线程池，把任务放进线程池中。

4.TimerTest是QTimer的定时器示例，通过信号和槽的连接，处理超时函数。注意:需要创建QT控制台应用程序。使用线程安全队列threadsafe_queue.h

5.SignalAndSlots探索QT信号和槽之间的关系。

6.QThreadDemo探讨的是QThread使用的两种方法。也使用到了信号和槽。将方法移动到线程中。

7.QProcessDemo 探讨的是利用QProcess执行cmd命令。

8.MyThreadDemo 探讨的是QThread，通过继承QThread类重载其run方法、信号和槽。

9.QtMyDaemon 探讨的是守护进程，把自身放入后台开机自启动。

10.matlab单例test

11.单例test，以及一些模式，代理模式、适配器模式、装饰模式

12.使用QProcess调用其他exe

13.QLineEditDemo探讨的是控件的事件，事件中的lamda表达式。

14.类A中包含类B的对象,定义回调函数,函数指针作为函数参数。

15.进度条、标签等空间使用，定时器的使用。 void timerEvent(QTimerEvent *event);

16.飞机动画

18.QTcp案例

19.STLDemo set集合模板;

20.QUdp案例

21.QTimer定时器操作 

23.操作数据库 mysql

24.车辆管理界面 ToolBox组建

25.thread在类的成员函数中的使用 需要thread和执行函数在一个类中,类成员函数作为回调函数bind一下。

com中是存放的是可执行程序所需要的.dll库。